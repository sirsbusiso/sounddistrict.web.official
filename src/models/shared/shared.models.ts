import { MusicRelease } from '../music-release/music-release.models';
import { MusicTrack } from '../music-track/music-track.models';

export enum ReleaseType {
  Single = 1,
  EP = 2,
  Album = 3,
}
